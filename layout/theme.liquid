<!DOCTYPE html>

<html data-wf-page="{{ wfPage }}" data-wf-site="689f3928b5dee013c4afd942" lang="{{ request.locale.iso_code }}" data-money-format="{{ shop.money_format | escape }}"><!--Last Published: 2025-08-30T18:02:15.447Z--><head>{%- liquid
    if headerSnippetPath
        include headerSnippetPath
    else
        render "__default-head"
    endif
-%}
{{content_for_header}}
{% if template contains 'customers' %}
    {{ "shopify_common.js" | shopify_asset_url | script_tag }}
{% endif %}
</head>
<body class="{{ bodyClasses }} {{ "template-" | append: request.page_type | handle }}">
{% if headerSnippetPath %}
    {{ content_for_layout }}
{% else %}
    {% include "backup-layout" %}
{% endif %}
</body></html>